apiVersion: v1
kind: Service
metadata:
  name: {{ include "simlab-api.fullname" . }}
  labels:
    {{- include "simlab-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simlab-api.selectorLabels" . | nindent 4 }}
